const gameData = {
    // Mapping of state index (Int) --> game state object. Where game state object contains:
    // - Text
    // - Image
    // - Mapping of choices { answer (String) : [next state index (Int), [personalities]] }
}